#include <iostream.h> //CODE BY ABHIGYAN KARMAKAR
#include <conio.h>
#include <stdio.h>
#include <string.h>
void main()
{
  clrscr();
  int a=1,b=1,c,d=0,i=0;
  char search[30];
  struct country
  {
    char name[30];
    char capital[40];
    long double gdp;
  }
  cont[50];
  for ( i=0 ; 1 ; i++)
  {
    cout<<i+1<<".    Country (Enter 'Exit' To Exit) : ";
    gets(cont[i].name);
    a=strcmpi("Exit",cont[i].name);
    if (a==0)
      break;
    cout<<"      Enter Capital : ";
    gets(cont[i].capital);
    cout<<"      Enter GDP/Gross National Income (IN BILLION $) : ";
    cin>>cont[i].gdp;
    clrscr();
  }
  SEARCH:
  clrscr();
  cout<<"Search For Country : ";
  gets(search);
  for (int j=0 ; j<=i ; j++)
  {
    b=strcmpi(search,cont[j].name);
    if (b==0)
    {
       d=j;
       break;
    }
    else
       continue;
  }
  if (b!=0)
  {
     cout<<"Country Is Not Present In Database!!! \nPress 1 To Try Again Or 0 To Exit";
     cout<<"\nChoice : ";
     cin>>c;
     if (c==1)
	goto SEARCH;
     else
	goto EXIT;
   }
  else if(b==0)
   {
      clrscr();
      cout<<"Name Of Country : "<<cont[d].name<<"\n";
      cout<<"Capital Of Country : "<<cont[d].capital<<"\n";
      cout<<"GDP (In Billion $) : "<<cont[d].gdp<<"\n";
      cout<<"\n\n\nTo Search Again Press 1 Press Anything Else to exit";
      cout<<"\nChoice : ";
      cin>>c;
      if (c==1)
	goto SEARCH;
      else
	goto EXIT;
   }
   getch();
   EXIT:
}






























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































