#include<iostream.h>
#include<conio.h>
#include<stdio.h>
#include<string.h>
void main()
{
  clrscr();
  char string[100],duplicate[100],revstr[100];
  cout<<"Enter String To Be Reversed : ";
  gets(string);
  int len;
  len=strlen(string);
  strcpy(duplicate,string);
  for (int i=0, lenght=len; i<len ; i++,lenght--)
    string[i]=duplicate[lenght-1];
    cout<<string;
  getch();
}
