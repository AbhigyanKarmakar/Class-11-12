#include<iostream.h> //CODE BY ABHIGYAN KARMAKAR
#include<conio.h>
void main()
{
  clrscr();
  cout<<"===================================PATTERN #1===================================";
  for (int i=65; i<=70; i++)
  {
      for (int j=65; j<=i; j++)
	cout<<char(j)<<" ";
      cout<<"\n";
  }
  cout<<"================================================================================";
  cout<<"===================================PATTERN #2===================================\n";
  for (int p=1; p<=7; p+=2)
  {
      for (int q=1; q<=p; q+=2)
	cout<<" ";
      for (int m=p; m<=7; m++)
	cout<<"$";
      cout<<"\n";
  }
  cout<<"================================================================================";
  cout<<"===================================PATTERN #3===================================\n";
  for (int r=1; r<=9; r=r+2)
  {
      for (int s=r; s<=7; s++)
	cout<<" ";
      if (r==1)
	cout<<"$";
      else if (r==3)
	cout<<"$   $";
      else if (r==5)
	cout<<"$       $";
      else if (r==7)
	cout<<"$           $";
      else if (r==9)
	for (int m=1; m<=8; m++)
	  cout<<"$ ";
      cout<<"\n";
  }
  cout<<"\n================================================================================";
  getch();
}