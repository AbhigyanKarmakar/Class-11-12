#include<iostream.h>
#include<conio.h>
void main()
{
clrscr();
int i=0,ua=0,ub=0,uc=0,fail=0;
while (i<=5)
{
  i++;
  ++fail;
  if (i==1)
  {
    ++ua;
    ++ub;
    ++uc;
  }
  if (i==2)
  {
    ++ua;
    ++ub;
    ++uc;
  }
  else if (i==4)
  {
    ++ub;
    ++uc;
  }
  else if (i==3)
  {
    ++ub;
    ++uc;
  }
  else if (i==5)
  {
    ++uc;
  }
}
cout<<"ua ="<<ua<<"\t"<<"ub= "<<ub<<"\n";
cout<<"uc ="<<uc<<"\t"<<"fail= "<<fail<<"\n";
getch();
}