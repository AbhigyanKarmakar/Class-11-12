#include<iostream.h>
#include<conio.h>
//#include<iomanip.h>
#include<string.h>
#include<dos.h>
#include<process.h>
 void main()
 {
  clrscr();
  char inp[100];
  int input=0,score=0,point[100],self=0,out[1000];
  do
  {
   inp[input]=getch();
   cout<<inp[input];
   score++;
   if(int(inp[input])==13)
   {
    cout<<"\n";
    cout<<inp[input];
    cout<<"Characters : ";
    point[self]=score-1;
    score=0;
    cout<<point[self];
    cout<<"\n";
    self++;
   }

  }
  while(inp[input]!='.');
  int j[10000];
  for(int zero=0 ; zero<=10000 ; zero++)
  {
   j[zero]=0;
  }
  for (int compare=0 ; compare<=10000 ; compare++)
  {
   for (int cin=0 ; cin<=10000 ; cin++)
  {
   if (inp[compare]==point[cin]-1)
   {
    j[c]=j[cin]+1;
   }
   else
   {
    continue;
   }

  }
  }

  for (int outo=0 ; outo<=1000 ; outo++)
  {
   if (j[outo]!=0)
   {
    cout<<"Your "<<outo<<" Name"<<" "<<j[outo]<<"\n";
   }
  }


  getch();/*
  clrscr();
  cout<<"\nExit ? Press Y for YES and N for NO";
  char ihwlisaannikki;
  SHAMITAB:
  ihwlisaannikki=getch();
  if (ihwlisaannikki=='y')
  exit (0);
  else
  goto SHAMITAB;*/




}












